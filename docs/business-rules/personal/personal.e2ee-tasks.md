# E2EE Upgrade Checklist

Checklist tracking the design, database, backend, and frontend tasks for the ChatBasket End-to-End Encryption (E2EE) upgrade.

## 1. Design & Specification
- [x] Select crypto library (`react-native-libsodium`)
- [x] Formulate encryption & decryption algorithms (`crypto_box_easy` for text, envelope encryption for media)
- [x] Define offline message queueing & encryption rules (always encrypt at send time, store plaintext locally)
- [x] Define cached key validation strategy (metadata sync, WS push, and future decryption failure recovery)
- [x] Define media upload and local storage rules (encrypted upload to Appwrite, decrypted storage in private sandbox)

## 2. Database Migrations
- [x] Audit all up/down migrations for re-run safety (idempotency)
- [x] Create migration to add `e2ee_public_key` column to `users` table (Migration 008)
- [x] Create/update migration to remove `CHECK (length(content) <= 5000)` constraint from `messages` table (Migration 006)
- [x] Apply migrations to development database

## 3. Backend Implementation (Next)
- [x] Add endpoint to upload/save user's `e2ee_public_key`
- [x] Add endpoint to fetch another user's `e2ee_public_key` by user ID
- [x] Update chat list / metadata API to include participant public keys for local caching
- [x] Update message delivery APIs to return sender public key (enables decryption)
  - [x] Text send: REST `POST /personal/chat/send`, WS `send_message`, and WS `new_message` payloads include `sender_e2ee_public_key`
  - [x] Message retrieval: `GET /personal/chat/messages` includes `sender_e2ee_public_key` for each message
  - [x] Pending relay/sync: `GET /personal/chat/pending` includes `sender_e2ee_public_key` for each message
  - [x] File messaging: REST `POST /personal/chat/upload` response and WS `new_message` file payload include `sender_e2ee_public_key`
  - [x] Chat list preview rule documented: if last-message preview decryption fails after key reset/change, client displays empty string (`""`)

## 4. Frontend Implementation (Phase 1: Native + Web E2EE parity)

Plan verified against the actual frontend code (Expo SDK 56, `react-native-libsodium` 1.7.0, `expo-secure-store` installed). Backend contract: `POST /personal/profile/update-e2ee-key`, `GET /personal/profile/get-e2ee-key?user_id=`, `other_user_e2ee_public_key` in chat list, `sender_e2ee_public_key` in all message/upload/WS payloads.

### 4.1 Models & Profile API Layer
- [x] Add `sender_e2ee_public_key?: string | null` to `MessageEntry` and `other_user_e2ee_public_key?: string | null` to `ChatEntry` (`src/lib/personalLib/models/personal.model.chat.ts`)
- [x] Add profile API methods following the existing `apiClient` pattern (`src/lib/personalLib/profileApi/personal.api.profile.ts`):
  - [x] `updateE2EEKey`: `apiClient.post('/personal/profile/update-e2ee-key', { e2ee_public_key })` (standard Base64, exactly 44 chars — backend rejects otherwise)
  - [x] `getE2EEKey`: `apiClient.get('/personal/profile/get-e2ee-key?user_id=<uuid>')` → `{ e2ee_public_key: string | null }` (payload/response types in `personal.model.profile.ts`)

### 4.2 Crypto Module (`src/lib/personalLib/e2ee/`)
- [x] `e2ee.crypto.ts` wrapping `react-native-libsodium` with the verified API constraints:
  - [x] Always `base64_variants.ORIGINAL` (standard Base64 with padding) — library default is URL-safe no-padding, which the backend rejects (defensive `?? 1` fallback for bare-Jest module load)
  - [x] Keys passed to `crypto_box_easy` / `crypto_box_open_easy` as `Uint8Array` (via `from_base64`)
  - [x] Decrypt output via `to_string(bytes)` — never the `'text'` outputFormat (throws on native JSI)
  - [x] Text wire format: `content = base64(nonce_24_bytes || ciphertext)`
  - [x] `isEncryptedContent()` heuristic to distinguish ciphertext from legacy/degraded plaintext
  - [x] Media helpers: `crypto_secretbox_keygen/easy/open_easy` (operate on `Uint8Array`, fits the existing XHR `arraybuffer` download path) + key wrapping with `crypto_box_easy`

### 4.3 Identity Keypair (PRIMARY-device-only generation — native + web alike)
- [x] `e2ee.keys.ts`: `initializeE2EEKeys()` — generate via `crypto_box_keypair()` ONLY when this device is the primary device (`authState.isPrimary === true`; loaded by `PersonalStorageGetDeviceStatus()` before key init in hydration). Secondary/unknown-status devices NEVER generate — they only hold already-persisted keys (or keys delivered by the Phase 2 key sync) and degrade gracefully without them. In-session promotion to primary (settings → `setCentralDevice` / device-status refresh) triggers generation via an `isPrimary.onChange` watcher; concurrent init calls coalesce so the keypair is never generated twice
- [x] Private key in `expo-secure-store` on native / `AppStorage.createSecure('secure-e2ee-storage')` encrypted vault on web (AES-GCM at rest, WebCrypto master key in IndexedDB); both backends lazy-imported — no native module/window touch at module load; never uploaded, no backup (strict E2EE — accept data loss)
- [x] `importIdentityKeypair()` — Phase 2 WebRTC key-sync entry point: replaces the device's own keypair with the account's shared identity key (recorded as already uploaded)
- [x] Upload public key via `updateE2EEKey` after profile exists; persist "uploaded" flag and retry on next launches until confirmed (+ retry on connectivity restore via `connectionWatcher`); upload is primary-gated — a device demoted before its upload was confirmed never overwrites the current primary's registered key
- [x] Wire into `hydratePersonalModules()` (`src/lib/storage/storage.init.ts`) after `initChatStorage()`; must not block hydration on network failure
- [x] Logout flow: key deletion added to `clearAuthState()` personal-storage cleanup (`storage.auth.ts`) + not-logged-in safety net in `storage.init.ts`; pending messages are fetched+decrypted earlier in the logout flow

### 4.4 Persistent Key Registry (never "cache")
- [x] Native: add `user_keys` table (`user_id` TEXT PK, `e2ee_public_key` TEXT, `updated_at` TEXT) to `initChatStorage()` in `chat.storage.native.ts` (`CREATE TABLE IF NOT EXISTS`, same idempotent style as `chats`/`messages`)
- [x] Web parity: `DATA_DB_VERSION` 3→4, `user_keys` object store (keyPath `user_id`) in `chat.storage.web.ts` using existing `idbPut`/`idbGet` helpers
- [x] Re-export `getUserE2eePublicKey`, `setUserE2eePublicKey`, `clearAllUserE2eeKeys` through the `chat.storage.ts` facade
- [x] Registry write sources (metadata sync — active validation):
  - [x] Chat list: persist `other_user_e2ee_public_key` keyed by `other_user_id` in `$chatListState.setChats()` (covers both callers: WS `refreshChatsAuthoritatively` and home screen `getUserChats()`)
  - [x] Incoming messages: persist `sender_e2ee_public_key` inside the shared ingress processor (4.6)
  - [x] Send-time fallback: `getE2EEKey(user_id)` when registry has no entry; `null` result → plaintext degradation

### 4.5 Text Encryption on Send (`src/lib/personalLib/chatApi/outbox.queue.ts`)
- [x] Outbox/local queue keeps plaintext (already the case — `enqueueTextMessage` stores raw content); encrypt only at send time when online
- [x] In `sendText()`: `encryptOutgoingText(recipient_id, content)` encrypts the `ChatTransport.sendMessage` payload — single hook covers both WS `send_message` and REST `POST /personal/chat/send` (WS-first transport with REST fallback)
- [x] Promotion fallback flipped to `content: msg.content || response.content || ''` — sender cannot decrypt own `crypto_box` ciphertext echoed by the server
- [x] `swapTempIdToRealId` and `promoteTempMessage` never persist ciphertext for own messages (`sentMessage.content` prefers local plaintext)
- [x] Known accepted limitation documented: sender's other devices receiving own (`is_from_me`) messages via WS/pending sync get ciphertext they cannot decrypt — displayed as `""`; multi-device self-copy strategy deferred

### 4.6 Text Decryption on Receive — single shared ingress processor
- [x] `processIncomingMessages(entries, { resolveSenderId })` in `e2ee.service.ts`: saves `sender_e2ee_public_key` to registry + decrypts text content (skip `is_from_me`, skip non-encrypted content) — decryption failure leaves content as `""` sentinel, never ciphertext
- [x] Wired into ALL FOUR ingress paths (each persists before ACK):
  - [x] WS bridge `handleNewMessage` (`ws.event.bridge.ts`): before `insertMessage(msg)` (persist happens before active-chat add and media download)
  - [x] REST history load: inside `$chatMessagesState.setMessages()` before persistence (covers `loadMessages` in `[chat_id].tsx`; idempotent for local-storage reloads)
  - [x] Pagination: inside `prependMessages()` before persistence — path missed by the earlier attempt
  - [x] Pending relay sync `syncPendingMessages()`: after status normalization, before `ChatStorage.insertMessages` (decrypt-then-persist-then-ACK preserved)
- [x] Chat list previews in `setChats()` via `processIncomingChats()`: registry sync + decrypt incoming `last_message_content` before `normalizeChatEntries`/persist; failure → `""` per spec

### 4.7 Media Envelope Encryption (start only after text E2EE verified end-to-end)
- [x] Upload (`outbox.queue.ts` `sendFile`/`buildFormDataForRetry`):
  - [x] `enqueueFileMessage` staging kept as-is (plaintext private copy via `copyFileToPrivateDir` = sender's local viewable file)
  - [x] Native: staged bytes read via SDK 56 `File.arrayBuffer()`, encrypted with fresh `crypto_secretbox` key into a cache-dir temp file, appended to FormData as `<name>.enc` with the ORIGINAL file MIME (backend `validateFileType` checks the part Content-Type against `message_type` — `image/*`/`video/*`/`audio/*`; bytes are ciphertext either way, the `.enc` name suffix stays the encryption marker)
  - [x] Web: staged IndexedDB blob encrypted via `encryptOutgoingMediaBlob` (fresh secretbox key, in-memory encrypted Blob uploaded as `<name>.enc` with the ORIGINAL file MIME — same backend part-MIME validation rationale as native; staged plaintext blob never touched; no-op `cleanup()` keeps the call sites uniform)
  - [x] Wrapped key `base64(nonce || crypto_box_easy(symmetricKey))` sent as upload `caption` → stored as message `content` by the backend; sender's local row keeps original `file_name`/`file_mime_type`/`file_size` (local-first metadata)
  - [x] Encrypted temp file deleted in `finally` after upload settles; sender's staged plaintext copy (`msg.local_uri`) never touched
- [x] Download/decrypt (`src/lib/personalLib/fileSystem/file.download.ts`):
  - [x] `downloadForNative` decrypts in-memory between the XHR `arraybuffer` and `destFile.write()`: `decryptIncomingMediaBytes()` unwraps the key from `msg.content` and opens the secretbox; plaintext written to `chatFiles/<messageId><ext>`
  - [x] `downloadForWeb` decrypts in-memory between the fetch stream and `storeMediaBlob()` — ciphertext never reaches IndexedDB; decryption failure throws before the store (same no-ACK discipline)
  - [x] `local_uri` keeps pointing at the decrypted file → zero changes needed in viewers/scroll
  - [x] ACK discipline preserved — decryption failure throws inside the existing reject path and counts as download failure (no ACK on primary)
  - [x] Incoming `.enc` metadata normalized in the ingress processor (strip `.enc` from `file_name`, restore MIME default by type) before persistence
- [x] Existing "Save to Gallery" flow keeps working unchanged (operates on decrypted `local_uri`)

### 4.8 Verification
- [x] `npx tsc --noEmit` in `chatbasket` — clean after every task and at completion
- [x] Automated dummy-data test suite (`src/__tests__/lib/e2ee/e2ee_dummy_data.test.ts`, 31 tests): real libsodium (WASM `libsodium-wrappers-sumo` behind the `react-native-libsodium` mock) driven with fake Alice/Bob identities — keypair format (44-char standard Base64), text wire-format round trips (unicode/emoji), send hook (registry hit / `get-e2ee-key` fallback / plaintext degradation), ingress processor (decrypt, `""` sentinel on tamper/wrong-key/missing-key, `is_from_me` blanking, idempotency, `.enc` metadata normalization), chat previews, media envelope round trip + tamper-throws (ACK discipline)
- [x] Automated media pipeline test suite (`src/__tests__/lib/e2ee/e2ee_media_pipeline.test.ts`, 18 tests): drives the REAL `encryptOutgoingMediaFile` and `downloadIncomingFile` control flow with an in-memory `expo-file-system` mock + scriptable XHR — outgoing `.enc` temp copy (recipient unwrap+decrypt round trip, staged plaintext never touched, `cleanup()` idempotent, fresh keys per upload, degradation to null for no-recipient-key/no-keypair/unreadable file), incoming native download (decrypt-before-write so ciphertext never reaches disk, tamper/missing-key/HTTP-error reject WITHOUT write = no ACK, failed decrypt doesn't poison retries, legacy plaintext + `is_from_me` pass-through, concurrent dedupe, idempotent re-download skip), plus full sender→relay→recipient composition (upload-prep output byte-exact through the download pipeline). Enabled by a jest-only babel `env.test` transform (`import(x)` → `Promise.resolve(require(x))` in `babel.config.js`) — Metro builds unaffected
- [x] Web-parity test suite (`src/__tests__/lib/e2ee/e2ee_web_parity.test.ts`, 28 tests): REAL `e2ee.keys` + `e2ee.service` under `Platform.OS === 'web'` with a mocked encrypted AppStorage vault — key lifecycle with PRIMARY-device-only generation (generate+persist+upload only when `authState.isPrimary === true`; NEVER on secondary or unknown status; generation fires on in-session promotion via the `isPrimary` watcher; no regeneration across launches; upload-failure retry; demoted device never uploads a pending key; Phase 2 `importIdentityKeypair` hook; vault load on relaunch regardless of primary status; logout vault deletion), outgoing text (plaintext degradation without keys, native-equivalent encryption with keys, `get-e2ee-key` fallback), incoming text (decrypt, "" blanking without keys — never Base64 garbage, `is_from_me` blanking, plaintext pass-through, registry sync), chat previews, and blob media envelope (byte-exact round trip, degradation to null, file-URI variant native-only, incoming flag + in-memory decrypt)
- [x] Detailed real-device trace logging (`e2ee.log.ts`): every E2EE step emits a trace line for `adb logcat` / Metro / browser console — key lifecycle (init start with platform + primary status, generation/load/READY with key fingerprints, upload skip/send/confirm, promotion watcher arm/fire, import, logout deletion), registry updates (previous→new fingerprint), recipient key resolution (registry hit / `get-e2ee-key` fetch / no-key), send decisions (ENCRYPTED with char counts vs PLAINTEXT degradation + reason), ingress per-message outcomes + batch summary (decrypted/plaintext/own-echo/failed/media-normalized counts), chat-list preview summary, media encrypt/decrypt stages with byte counts. PRIVACY: never logs private keys, plaintext content, or full ciphertext — public-key fingerprints (first 8 Base64 chars) and lengths only; auto-silenced under Jest; `E2EE_VERBOSE` master switch for production
- [x] Real two-user web test — incident found & FIXED (web cold-start detection bug): user B's encrypted text reached user A but was classified as plaintext (`Ingress: {decrypted: 0, plaintext: 1}`) and the raw 56-char Base64 ciphertext was persisted/rendered. Root cause (verified by manually reading `react-native-libsodium/src/lib.ts`): on web ALL exports are Proxy-backed and `undefined` until the WASM `ready` resolves (`export let crypto_box_NONCEBYTES = sodium.crypto_box_NONCEBYTES` at module-eval → `undefined`; the real value is assigned only inside the ready handler), so `MIN_ENCRYPTED_BYTES = crypto_box_NONCEBYTES + 16` froze as NaN and `isEncryptedContent()` returned false for every ciphertext. Fix: protocol-frozen constants hardcoded in `e2ee.crypto.ts` (nonce 24 / MAC 16, documented rationale) and `isEncryptedContent()` rewritten as pure JS length/padding math (no sodium call — detection works even before WASM ready); the previously NaN-disabled "payload too short" guards in `decryptText`/`unwrapMediaKey` are effective again. Jest mocks in all 3 E2EE suites switched to lazy getters (`undefined` pre-ready — faithful to the web Proxy, so this bug class now fails tests instead of shipping). New regression suite `src/__tests__/lib/e2ee/e2ee_cold_start_detection.test.ts` (8 tests): pre-ready detection of the EXACT incident ciphertext + short-message (1–3 char / emoji → minimal 56-char wire) round trips through the real send hook and ingress processor. Already-persisted ciphertext rows self-heal: history loads re-run the ingress processor and decrypt via the stored `sender_e2ee_public_key`
- [x] Display-time safety net — "never show raw cipher" (follow-up to the cold-start incident): `toDisplaySafeText(content, messageType)` in `e2ee.crypto.ts` is the LAST line of defense applied at the only two render choke points — the `MessageBubble` `text` prop (`[chat_id].tsx`, bubbles + media captions) and `getPreviewText()` (`util.chatPreview.ts`, every chat-list/screen preview, both `file_name` and `content` branches). Policy: cipher-looking TEXT (or unknown type) renders "Failed to load" (never Base64 garbage); cipher-looking MEDIA content renders "" (the wrapped media key travels in `content`/caption by design — transport metadata, not a caption; this also fixed a live leak where received encrypted attachments showed the 96-char wrapped key as their caption). Plaintext, legit file names, and unsent markers pass through untouched; detection is pure JS so the guard works before web WASM is ready. Regression suite `src/__tests__/lib/e2ee/e2ee_display_guard.test.ts` (11 tests): pre-ready incident-ciphertext replay, wrapped-key caption masking across all media types, preview integration incl. the ChatListItem `file_name` reuse path, plaintext/emoji/short-Base64 pass-through, real `crypto_box` wire + real wrapped key masking after ready
- [x] Real two-user web test — two more incidents found & FIXED (refresh self-heal + encrypted file MIME/open routing): (A) "empty bubble after refresh": sender refreshed while the recipient was offline — the relay still held the undelivered message, so history/pending returned the sender's OWN message as ciphertext (`is_from_me: true`); the ingress processor blanked it to "" and the persist path overwrote the locally stored plaintext → empty bubble with time+tick. Fix: the own-echo branch and the chat-list own-preview branch now RESTORE the locally persisted plaintext row before falling back to blanking — new `ChatStorage.getMessagesByIds(ids)` (native: SQLite `IN` select; web: per-id IDB get + `decryptEntry`, requests fired before awaiting to keep the tx active; facade relay), one batched lookup per ingress/chat-list batch, fully defensive (lookup throw / missing impl → old "" blanking; ciphertext or empty local rows are never resurrected; restore works even before this device's keys load). (B) "file message only offers a download": encrypted uploads travel as `<name>.enc` + application/octet-stream, so the persisted MIME stayed octet-stream after decrypt and browsers download instead of rendering; tapping a file with no local copy would also have opened the REMOTE ciphertext URL. Fix: `.enc` ingress normalization now derives the real MIME from the restored file name (`mime.lookup` → per-type default fallback); the chat-screen file-open handler routes encrypted files through download+decrypt to a local copy FIRST (persists `local_uri` to storage + in-memory state, opens the local plaintext copy, alert + no-open on failure); the web `idb://` open path heals stale octet-stream blob typing (message MIME / file-name lookup) before `window.open`. Regression suite `src/__tests__/lib/e2ee/e2ee_refresh_restore.test.ts` (16 tests): own-echo restore incl. keys-not-loaded race, no-row/cipher-row/empty-row → "", lookup-throw degradation, single batched lookup + skip-when-none, own-preview restore variants, incoming-preview decrypt sanity, MIME derivation (pdf/png/unknown-ext fallback/server-MIME preserved)
- [x] Real two-user web test — two more incidents found & FIXED (media-over-E2EE follow-ups): (A) "image/video stuck pending until refresh": encrypted uploads traveled as `application/octet-stream`, but the backend's `validateFileType` requires the multipart part MIME to match the message type (`image/*`, `video/*`, `audio/*`) → every encrypted image/video upload was rejected; it only "sent" right after a refresh because the upload slipped through PLAINTEXT before keys finished loading. Frontend-only fix in `buildFormDataForRetry` (web + native): the encrypted part now carries the ORIGINAL file MIME (`msg.file_mime_type`) — bytes stay ciphertext, `.enc` stays the ingress marker, and `.enc` normalization keeps the echoed real MIME (only fills octet-stream). (B) "sender's own file opens as ciphertext after refresh": `downloadMediaBatch` re-downloaded own media (server entries carry no `local_uri`), nulled the stored sender `local_uri` via full-row `insertMessages`, and — because own media was excluded from decryption — stored the raw ciphertext as the local copy. Fixes: (1) `downloadMediaBatch` batch-restores the stored `local_uri` (`ChatStorage.getMessagesByIds`) before persisting/downloading, so the sender's staged plaintext copy survives refreshes (also stops pointless re-downloads for everyone); (2) own media is now decryptable — `isEncryptedMediaMessage` includes `is_from_me`, and the new `resolveMediaUnwrapKey` unwraps with the RECIPIENT's registry key (crypto_box is bidirectional: `(recipientPub, myPriv)` derives the same shared secret), both download paths pass the resolved key, and failures still throw (no ACK, ciphertext never written). Regression coverage: `e2ee_web_parity` (own-media flagging, resolver, sender self-decrypt round trip incl. legacy-default-fails sanity), `e2ee_media_pipeline` (own-echo download writes plaintext; unresolvable recipient key → reject without write), `e2ee_dummy_data` (own-media unwrap round trip) — E2EE suites now 6 suites / 116 tests, all green
- [x] Real two-user web test — incident found & FIXED (home-screen previews vanish after a page refresh): chats whose last message was a photo/file showed a BLANK preview (status icon only) after a refresh, while live-session previews were fine. Root cause (frontend): the server-side chat-list preview content for an encrypted media message is the WRAPPED MEDIA KEY (own and incoming alike), and `processIncomingChats()` skipped every non-text preview — the wrapped key stayed as `last_message_content`, `ChatListItem` reuses that field as `file_name`, and the display guard correctly masked the cipher-looking value to "" (the status icon still rendered because it checks the raw content). In-session previews were fine because the WS bridge stores `getPreviewText(msg)` = the plaintext file name. Fix in `e2ee.service.ts`: the restore pre-pass now also collects media previews (both sides) and a new media branch restores the ingress-normalized plaintext `file_name` from the local message row (`ChatStorage.getMessagesByIds`), blanking only when no readable local copy exists; own-text restore unchanged. Hardening: new `whenKeyInitSettled()` in `e2ee.keys.ts` (returns the coalesced in-flight init promise or resolves instantly) is awaited at the top of BOTH `processIncomingMessages` and `processIncomingChats`, so a cold start / refresh can no longer race key loading and persist blanked content that would decrypt fine a moment later (bounded: sodium ready + keystore reads + at most one caught upload attempt; never throws). Regression tests added to `e2ee_refresh_restore.test.ts` (INCIDENT C, +5 tests → suite now 21): own/incoming media preview restored to the local file name, no-local-copy → "" (never ciphertext), plaintext media preview untouched without a storage lookup, restore works while keys are still loading; the 4 suite mocks of `e2ee.keys` expose `whenKeyInitSettled`
- [x] Real two-user web test — incident found & FIXED (one attachment fails to download after a page refresh with "missing unwrap public key", then succeeds moments later): local message rows do NOT persist `sender_e2ee_public_key`, so encrypted media re-fed from LOCAL storage (chat open → `setMessages` → `downloadMediaBatch` → `downloadForWeb`/`downloadForNative`) reached `resolveMediaUnwrapKey` without a payload key → null → `decryptIncomingMediaBytes` threw (correctly: no ACK, no ciphertext written); the same file decrypted later because a server-fed path (pending sync / fresh history) carried the key. Fix in `e2ee.service.ts`: `resolveMediaUnwrapKey` now falls back for incoming messages — new defensive `resolveChatCounterpartUserId(chat_id)` (local chat row via `ChatStorage.getChats()` → `other_user_id`; missing impl / storage failure → null) feeds `resolveRecipientPublicKey` (registry → `get-e2ee-key`, result persisted), which the chat list keeps current; the payload key still wins when present (key-rotation correctness), own-message path unchanged. Regression tests in `e2ee_refresh_restore.test.ts` (INCIDENT D, +6 → suite now 27; suite mock gained `getChats`): payload-key preference (no storage lookup), chat-row→registry fallback, full envelope decrypt round trip with the fallback-resolved key, registry-miss→endpoint fallback (persisted), no-chat-row → null without endpoint call, chat-lookup-throw → null — E2EE suites now 6 suites / 127 tests, all green
- [x] Real two-user web test — incident found & FIXED (home-screen preview stays EMPTY after the FIRST app open even though pending messages sync fine, until a manual refresh): ordering bug — `setChats` runs BEFORE `syncPendingMessages` (it *triggers* it), so the E2EE chat-list pass has no local plaintext row yet to restore an encrypted preview from and blanks it to `""`; the pending-sync preview-update block in `personal.state.chat.ts` then never healed the blank because (a) the guard `if (serverPreview === null || serverPreview === '') continue;` skipped blank previews entirely and (b) the strict `isNewer` (`>`) check failed — the blanked server preview references the SAME pending message (equal timestamps). After a manual refresh the local row exists, so the chat-list restore pass healed it — exactly the reported behavior. Fix in `syncPendingMessages`: new `healsBlankedPreview` = blank/absent in-memory preview + non-empty display-safe `getPreviewText(lastMsg)` → bypasses both the blank guard and the strict `isNewer` check so the freshly decrypted pending message fills the preview; unreadable messages keep the blank (never ciphertext — `getPreviewText` is display-guarded), and non-blank previews keep the old rules untouched. Regression suite `src/__tests__/state/chat/pending_preview_heal.test.ts` (6 tests, real `e2ee.service`, ack-race harness mocks): incident replay (blank `""` + same-timestamp message healed), null-preview heal, media file-name heal, unreadable → stays blank, non-newer readable preview untouched, newer message still updates
- [ ] Text E2EE manual test matrix: live WS message, offline→pending sync, history load, pagination, chat list preview, key-missing (plaintext degradation)
- [ ] Media E2EE manual test: image/video/file upload + download + decrypt + view + "Save to Gallery"
- [x] Existing tests verified — full jest run: 155 passed / 6 failed across 11 suites (161 total); the 6 failures are the pre-existing `is_contactable`/`profile_avatar` issues (unrelated to E2EE). Strictly better than the original 8-failure baseline: the jest-only dynamic-import transform fixed 2 tests that were previously failing on the same VM limitation (verified by re-running those suites with the transform bypassed → 8 failures). `ack_race_condition` and `avatar_caching` pass; `npx tsc --noEmit` clean

## 5. Phase 2: WebRTC Key Sync (shared identity key)

Note: in Phase 1 the account keypair is generated ONLY on the primary device (native or web — same pipeline). Secondary devices hold no keys and degrade gracefully (plaintext send, `""` on undecryptable content) until Phase 2 delivers the primary's identity key over WebRTC — the frontend entry point (`importIdentityKeypair`) already exists, and a device promoted to primary in-session generates its keypair on promotion.

- [ ] Implement `key_sync_request` WebSocket signal relay on server
- [ ] Implement WebRTC SDP/ICE candidate exchange signaling on server
- [ ] Implement WebRTC P2P DTLS data channel setup on native app
- [ ] Implement automatic key transfer over P2P data channel
- [ ] Store the synced key via `importIdentityKeypair` (encrypted vault) — enables decrypting own-message echoes across devices

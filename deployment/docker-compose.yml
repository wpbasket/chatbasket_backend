services:
  api:
    image: ghcr.io/wpbasket/chatbasket-api:latest
    container_name: chatbasket-api
    restart: always

    # 🔥 Hard container memory limit (2GB Oracle VM)
    mem_limit: 1800m

    env_file:
      - .env

    # Go internal memory control
    environment:
      - GOMEMLIMIT=1600MiB

    expose:
      - "8080"

    # 🔥 Healthcheck (checks your /api/healthz endpoint)
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/api/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

    # 🔥 Official Docker log rotation
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  nginx:
    image: nginx:alpine
    container_name: chatbasket-nginx
    restart: always

    # Nginx reverse proxy memory limit (production standard: 1.5-2x peak usage)
    mem_limit: 256m

    ports:
      - "80:80"
      - "443:443"

    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/ssl/cloudflare:/etc/ssl/cloudflare:ro

    depends_on:
      - api

    # Log rotation for nginx container too
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"